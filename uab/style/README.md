**[EN]** Customization can be done in the `ciclope.css`. Avoid editing CSS files under `/libraries`.

**[PT]** Customização de CSS pode ser incluida no `ciclope.css`. Evite editar os CSS dentro do `/libraries`.
